\section{Experimental Results}\label{sec:task2-results}

The results below were read directly from \path{task2/output/camera_params.npz} for the 18 real JPEG files in \path{task2/images_jpg/*.jpg}. The run used an \(8\times5\) checkerboard, a 3.0 cm square size, and \texttt{--min-images 3}. These values are recorded outputs, not estimates or placeholders.

\begin{equation}
\mathbf K=
\begin{bmatrix}
4082.15290893 & 0 & 2883.66417823\\
0 & 4083.58657686 & 2107.20293734\\
0 & 0 & 1
\end{bmatrix}.
\label{eq:task2-result-k}
\end{equation}

\begin{equation}
\mathbf d=
\left[
0.20245123,\;-0.29420837,\;-0.00326679,\;0.00308671,\;-1.04554325
\right],
\label{eq:task2-result-distortion}
\end{equation}
where \(\mathbf d\) follows the common OpenCV order
\((k_1,k_2,p_1,p_2,k_3)\).

\begin{table}[htbp]
    \centering
    \caption{Mobile-camera calibration results read from the official NPZ file}
    \label{tab:task2-results}
    \begin{tabular}{lll}
        \hline
        Category & Metric & Recorded value \\
        \hline
        Data & Input pattern & \path{task2/images_jpg/*.jpg} \\
        Data & Candidate images & 18 \\
        Data & Valid calibration images & 18 \\
        Data & Failed images & 0 (read, corner, and size failures) \\
        Data & Image size (width \(\times\) height) & \(5712\times4284\) \\
        Configuration & Checkerboard inner corners & \(8\times5\) \\
        Configuration & \path{square_size} & 3.0 cm \\
        Intrinsics & Camera matrix \(\mathbf K\) & See Equation~\ref{eq:task2-result-k} \\
        Intrinsics & \(f_x\) & 4082.15290893 \\
        Intrinsics & \(f_y\) & 4083.58657686 \\
        Intrinsics & \(c_x\) & 2883.66417823 \\
        Intrinsics & \(c_y\) & 2107.20293734 \\
        Distortion & \((k_1,k_2,p_1,p_2,k_3)\) & See Equation~\ref{eq:task2-result-distortion} \\
        Error & OpenCV RMS (pixels) & 2.06424401 \\
        Error & Mean pointwise reprojection error (pixels) & 1.65696931 \\
        Error & Mean per-image RMS (pixels) & 1.90750642 \\
        Extrinsics & Rotation/translation shape & \(18\times3\times1\); translations in cm \\
        \hline
    \end{tabular}
\end{table}

OpenCV RMS is the global root-mean-square error returned by \texttt{cv2.calibrateCamera}. The mean pointwise reprojection error is \path{mean_reprojection_error}, averaged over all \(18\times40=720\) valid corners. The mean per-image RMS is \path{mean_per_image_rms}, the arithmetic mean of 18 per-image RMS values. These definitions are distinct. The per-image array is stored in \path{per_image_reprojection_error}; poses are stored in \path{rotation_vectors} and \path{translation_vectors}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.47\linewidth]{task2-corners-IMG_5486.jpg}
    \hfill
    \includegraphics[width=0.47\linewidth]{task2-undistorted-IMG_5486.jpg}
    \caption{Representative results for \texttt{IMG\_5486.jpg}: the left image shows the detected corners, and the right image is the undistorted image produced with the same calibration. The figures come from the temporary visualization run; numerical results remain those in \texttt{task2/output/camera\_params.npz}.}
    \label{fig:task2-visual-results}
\end{figure}

Figure~\ref{fig:task2-visual-results} shows one of the 18 valid views rather than repeating all outputs. The left image checks coverage of the \(8\times5\) inner corners, while the right image illustrates the undistortion result. Both were generated with \texttt{--save-corners --undistort}.
